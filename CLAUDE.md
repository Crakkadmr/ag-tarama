# CLAUDE.md

AgTarama — .NET 10 WPF ağ tarama uygulaması (Network Sniffer markası).
Proje referansı: `AGENTS.md` (kısa index) → `docs/` klasörü (ui, services, partials, licensing, architecture).
Görev gerektirdiğinde ilgili `docs/` dosyasını oku; her session başında hepsini okuma.
Markdown dosyalar otomatik güncellenmez — yalnızca "md güncelle" komutuyla güncelle.
Bu kural hem Claude Code hem Codex CLI için geçerlidir.
