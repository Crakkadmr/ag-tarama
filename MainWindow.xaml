<Window x:Class="AgTarama.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Ağ Tarama Programı" Height="650" Width="1000"
        MinHeight="500" MinWidth="750"
        WindowStartupLocation="CenterScreen"
        Background="#0D1117">

    <Window.Resources>
        <!-- Buton stili -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="Foreground" Value="#58A6FF"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1F2937"/>
                                <Setter Property="BorderBrush" Value="#58A6FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D3B66"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#484F58"/>
                                <Setter Property="BorderBrush" Value="#21262D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Aktif panel butonu (yeşil çerçeve, hover'da arka plan değişir ama çerçeve korunur) -->
        <Style x:Key="ActiveActionButton" TargetType="Button">
            <Setter Property="Background" Value="#0D1F12"/>
            <Setter Property="Foreground" Value="#3FB950"/>
            <Setter Property="BorderBrush" Value="#3FB950"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#162B1C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0F3D20"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Birincil aksiyon butonu (Taramayı Başlat) -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#1A4A2E"/>
            <Setter Property="Foreground" Value="#3FB950"/>
            <Setter Property="BorderBrush" Value="#238636"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#22623C"/>
                                <Setter Property="BorderBrush" Value="#3FB950"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0F3D20"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#484F58"/>
                                <Setter Property="BorderBrush" Value="#21262D"/>
                                <Setter Property="Background" Value="#161B22"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Durdur butonu -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#3D1A1A"/>
            <Setter Property="Foreground" Value="#F85149"/>
            <Setter Property="BorderBrush" Value="#8B1A1A"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4D2020"/>
                                <Setter Property="BorderBrush" Value="#F85149"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2D0F0F"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#484F58"/>
                                <Setter Property="BorderBrush" Value="#21262D"/>
                                <Setter Property="Background" Value="#161B22"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ScrollBar stili -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="6"/>
        </Style>

        <!-- Ping giriş kutusu -->
        <Style x:Key="PingInputBox" TargetType="TextBox">
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="Foreground" Value="#E6EDF3"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="#58A6FF"/>
            <Setter Property="SelectionBrush" Value="#1F6FEB"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#58A6FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Hızlı seçim chip butonu -->
        <Style x:Key="ChipButton" TargetType="Button">
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="Foreground" Value="#8B949E"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,0,6,6"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1F2937"/>
                                <Setter Property="BorderBrush" Value="#58A6FF"/>
                                <Setter Property="Foreground" Value="#58A6FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D3B66"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Seçili chip butonu (SNMP versiyon toggle — ChipButton'dan SONRA tanımlanmalı) -->
        <Style x:Key="SelectedChipButton" TargetType="Button" BasedOn="{StaticResource ChipButton}">
            <Setter Property="Background"  Value="#0D3B66"/>
            <Setter Property="Foreground"  Value="#58A6FF"/>
            <Setter Property="BorderBrush" Value="#1F6FEB"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="220"/>
        </Grid.ColumnDefinitions>

        <!-- SOL: Chat alanı -->
        <Grid Grid.Column="0" Margin="16,16,8,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Başlık -->
            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Text="AG TARAMA"
                           Foreground="#58A6FF"
                           FontSize="20"
                           FontWeight="Bold"
                           FontFamily="Consolas"/>
                <TextBlock x:Name="StatusText"
                           Text="● Hazır"
                           Foreground="#3FB950"
                           FontSize="11"
                           FontFamily="Consolas"
                           Margin="0,2,0,0"/>
            </StackPanel>

            <!-- Mesaj alanı + Ping paneli -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition x:Name="PingCol" Width="0"/>
                </Grid.ColumnDefinitions>

                <!-- Ana chat -->
                <Border Grid.Column="0"
                        Background="#0D1117"
                        BorderBrush="#21262D"
                        BorderThickness="1"
                        CornerRadius="8">
                    <ScrollViewer x:Name="ChatScrollViewer"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  Padding="4,4,8,4">
                        <StackPanel x:Name="ChatPanel" Margin="8,8,4,8"/>
                    </ScrollViewer>
                </Border>

                <!-- Ping paneli (animasyonla açılır) -->
                <Grid Grid.Column="1" x:Name="PingPanel" ClipToBounds="True">
                    <Border Background="#0D1117"
                            BorderBrush="#21262D"
                            BorderThickness="1"
                            CornerRadius="8"
                            Margin="8,0,0,0">
                        <Grid Margin="14,14,14,14">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Başlık -->
                            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                                <TextBlock Text="◈ PING TESTİ"
                                           Foreground="#58A6FF"
                                           FontFamily="Consolas"
                                           FontSize="12"
                                           FontWeight="Bold"
                                           Margin="0,0,0,6"/>
                                <TextBlock Text="Hangi IP'ye atmak istiyorsunuz?"
                                           Foreground="#8B949E"
                                           FontFamily="Consolas"
                                           FontSize="11"
                                           TextWrapping="Wrap"/>
                            </StackPanel>

                            <!-- IP giriş alanı -->
                            <Grid Grid.Row="1" Margin="0,0,0,16">
                                <TextBox x:Name="PingIpBox"
                                         Style="{StaticResource PingInputBox}"
                                         Padding="10,8,34,8"
                                         TextChanged="PingIpBox_TextChanged"
                                         KeyDown="PingIpBox_KeyDown"/>
                                <TextBlock x:Name="PingPlaceholder"
                                           Text="Örn: 192.168.1.1"
                                           FontFamily="Consolas"
                                           FontSize="13"
                                           Foreground="#484F58"
                                           VerticalAlignment="Center"
                                           Margin="12,0,0,0"
                                           IsHitTestVisible="False"/>
                                <TextBlock x:Name="PingValidasyonIkonu"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center"
                                           Margin="0,0,10,0"
                                           FontFamily="Consolas"
                                           FontSize="15"
                                           FontWeight="Bold"
                                           IsHitTestVisible="False"/>
                            </Grid>

                            <!-- Hızlı seçenekler -->
                            <StackPanel Grid.Row="2">
                                <TextBlock Text="HIZLI SEÇİM"
                                           Foreground="#484F58"
                                           FontFamily="Consolas"
                                           FontSize="10"
                                           Margin="0,0,0,8"/>
                                <WrapPanel>
                                    <Button Style="{StaticResource ChipButton}" Tag="192.168.1.1"   Content="192.168.1.1"   Click="PingHizliBtn_Click"/>
                                    <Button Style="{StaticResource ChipButton}" Tag="192.168.1.254" Content="192.168.1.254" Click="PingHizliBtn_Click"/>
                                    <Button Style="{StaticResource ChipButton}" Tag="8.8.8.8"       Content="8.8.8.8"       Click="PingHizliBtn_Click"/>
                                    <Button Style="{StaticResource ChipButton}" Tag="1.1.1.1"       Content="1.1.1.1"       Click="PingHizliBtn_Click"/>
                                    <Button Style="{StaticResource ChipButton}" Tag="8.8.4.4"       Content="8.8.4.4"       Click="PingHizliBtn_Click"/>
                                    <Button Style="{StaticResource ChipButton}" Tag="google.com"    Content="google.com"    Click="PingHizliBtn_Click"/>
                                </WrapPanel>
                            </StackPanel>

                            <!-- Başlat -->
                            <Button x:Name="PingBaslatBtn"
                                    Grid.Row="3"
                                    Style="{StaticResource PrimaryButton}"
                                    IsEnabled="False"
                                    Height="36"
                                    HorizontalContentAlignment="Center"
                                    Click="PingBaslatBtn_Click"
                                    Margin="0,8,0,4">
                                <TextBlock Text="◈  Ping Başlat"
                                           FontFamily="Consolas"
                                           FontSize="12"
                                           FontWeight="Bold"/>
                            </Button>

                            <!-- Ping sonuç kutusu -->
                            <Border x:Name="PingResultBorder"
                                    Grid.Row="4"
                                    Background="#0D1117"
                                    BorderBrush="#21262D"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Margin="0,8,0,0"
                                    Visibility="Collapsed">
                                <ScrollViewer x:Name="PingResultScroll"
                                              MaxHeight="180"
                                              VerticalScrollBarVisibility="Auto"
                                              HorizontalScrollBarVisibility="Disabled">
                                    <StackPanel x:Name="PingResultPanel"
                                                Margin="10,8,10,8"/>
                                </ScrollViewer>
                            </Border>

                            <!-- Kapat -->
                            <Button Grid.Row="5"
                                    Content="✕  Kapat"
                                    Style="{StaticResource ActionButton}"
                                    Click="PingPanelKapat_Click"
                                    Margin="0,10,0,0"
                                    HorizontalContentAlignment="Center"
                                    Foreground="#8B949E"/>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Port Tara paneli (animasyonla açılır) -->
                <Grid Grid.Column="1" x:Name="PortPanel" ClipToBounds="True" Visibility="Collapsed">
                    <Border Background="#0D1117"
                            BorderBrush="#21262D"
                            BorderThickness="1"
                            CornerRadius="8"
                            Margin="8,0,0,0">
                        <Grid Margin="14,14,14,14">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Başlık -->
                            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                                <TextBlock Text="⊞ PORT TARA"
                                           Foreground="#58A6FF"
                                           FontFamily="Consolas"
                                           FontSize="12"
                                           FontWeight="Bold"
                                           Margin="0,0,0,6"/>
                                <TextBlock Text="IP ve port aralığı girin:"
                                           Foreground="#8B949E"
                                           FontFamily="Consolas"
                                           FontSize="11"
                                           TextWrapping="Wrap"/>
                            </StackPanel>

                            <!-- IP giriş -->
                            <Grid Grid.Row="1" Margin="0,0,0,8">
                                <TextBox x:Name="PortIpBox"
                                         Style="{StaticResource PingInputBox}"
                                         Padding="10,8,34,8"
                                         TextChanged="PortIpBox_TextChanged"
                                         KeyDown="PortIpBox_KeyDown"/>
                                <TextBlock x:Name="PortIpPlaceholder"
                                           Text="Hedef IP"
                                           FontFamily="Consolas"
                                           FontSize="13"
                                           Foreground="#484F58"
                                           VerticalAlignment="Center"
                                           Margin="12,0,0,0"
                                           IsHitTestVisible="False"/>
                                <TextBlock x:Name="PortIpValidasyon"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center"
                                           Margin="0,0,10,0"
                                           FontFamily="Consolas"
                                           FontSize="15"
                                           FontWeight="Bold"
                                           IsHitTestVisible="False"/>
                            </Grid>

                            <!-- Port aralığı giriş -->
                            <Grid Grid.Row="2" Margin="0,0,0,8">
                                <TextBox x:Name="PortAralikBox"
                                         Style="{StaticResource PingInputBox}"
                                         Padding="10,8,10,8"
                                         TextChanged="PortAralikBox_TextChanged"/>
                                <TextBlock x:Name="PortAralikPlaceholder"
                                           Text="Portlar: 1-1024, 80,443"
                                           FontFamily="Consolas"
                                           FontSize="11"
                                           Foreground="#484F58"
                                           VerticalAlignment="Center"
                                           Margin="12,0,0,0"
                                           IsHitTestVisible="False"/>
                            </Grid>

                            <!-- Hızlı seçim + Başlat -->
                            <Grid Grid.Row="3" Margin="0,0,0,4">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="HIZLI SEÇİM"
                                           Foreground="#484F58"
                                           FontFamily="Consolas"
                                           FontSize="10"
                                           Margin="0,0,0,6"/>
                                <WrapPanel Grid.Row="1" Margin="0,0,0,8">
                                    <Button Style="{StaticResource ChipButton}"
                                            Tag="21,22,23,25,53,80,110,135,139,143,443,445,587,993,995,1433,3306,3389,5900,8080"
                                            Content="Yaygın 20" Click="PortHizliBtn_Click"/>
                                    <Button Style="{StaticResource ChipButton}"
                                            Tag="80,443,8080,8443"
                                            Content="Web" Click="PortHizliBtn_Click"/>
                                    <Button Style="{StaticResource ChipButton}"
                                            Tag="22,3389"
                                            Content="SSH+RDP" Click="PortHizliBtn_Click"/>
                                    <Button Style="{StaticResource ChipButton}"
                                            Tag="80,443,554,8000,8080,37777"
                                            Content="Kamera" Click="PortHizliBtn_Click"/>
                                </WrapPanel>
                                <Button x:Name="PortBaslatBtn"
                                        Grid.Row="2"
                                        Style="{StaticResource PrimaryButton}"
                                        IsEnabled="False"
                                        Height="36"
                                        HorizontalContentAlignment="Center"
                                        Click="PortBaslatBtn_Click">
                                    <TextBlock Text="▶  Taramayı Başlat"
                                               FontFamily="Consolas"
                                               FontSize="12"
                                               FontWeight="Bold"/>
                                </Button>
                            </Grid>

                            <!-- Sonuç kutusu -->
                            <Border x:Name="PortResultBorder"
                                    Grid.Row="4"
                                    Background="#0D1117"
                                    BorderBrush="#21262D"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Margin="0,4,0,0"
                                    Visibility="Collapsed">
                                <ScrollViewer x:Name="PortResultScroll"
                                              MaxHeight="160"
                                              VerticalScrollBarVisibility="Auto"
                                              HorizontalScrollBarVisibility="Disabled">
                                    <StackPanel x:Name="PortResultPanel"
                                                Margin="10,8,10,8"/>
                                </ScrollViewer>
                            </Border>

                            <!-- Kapat -->
                            <Button Grid.Row="5"
                                    Content="✕  Kapat"
                                    Style="{StaticResource ActionButton}"
                                    Click="PortPanelKapat_Click"
                                    Margin="0,10,0,0"
                                    HorizontalContentAlignment="Center"
                                    Foreground="#8B949E"/>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Traceroute paneli -->
                <Grid Grid.Column="1" x:Name="TracePanel" ClipToBounds="True" Visibility="Collapsed">
                    <Border Background="#0D1117" BorderBrush="#21262D" BorderThickness="1" CornerRadius="8" Margin="8,0,0,0">
                        <Grid Margin="14,14,14,14">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                                <TextBlock Text="⇢ TRACEROUTE" Foreground="#58A6FF" FontFamily="Consolas" FontSize="12" FontWeight="Bold" Margin="0,0,0,6"/>
                                <TextBlock Text="Hedefe giden ağ yolunu göster:" Foreground="#8B949E" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"/>
                            </StackPanel>
                            <Grid Grid.Row="1" Margin="0,0,0,10">
                                <TextBox x:Name="TraceHedefBox" Style="{StaticResource PingInputBox}" Padding="10,8,34,8"
                                         TextChanged="TraceHedefBox_TextChanged" KeyDown="TraceHedefBox_KeyDown"/>
                                <TextBlock x:Name="TraceHedefPlaceholder" Text="Hedef IP veya hostname"
                                           FontFamily="Consolas" FontSize="12" Foreground="#484F58"
                                           VerticalAlignment="Center" Margin="12,0,0,0" IsHitTestVisible="False"/>
                                <TextBlock x:Name="TraceHedefValidasyon" HorizontalAlignment="Right"
                                           VerticalAlignment="Center" Margin="0,0,10,0"
                                           FontFamily="Consolas" FontSize="15" FontWeight="Bold" IsHitTestVisible="False"/>
                            </Grid>
                            <!-- Hızlı seçim -->
                            <StackPanel Grid.Row="2" Margin="0,0,0,8">
                                <TextBlock Text="HIZLI SEÇİM" Foreground="#484F58" FontFamily="Consolas" FontSize="10" Margin="0,0,0,6"/>
                                <WrapPanel>
                                    <Button Style="{StaticResource ChipButton}" Tag="8.8.8.8"       Content="8.8.8.8"       Click="TraceHizliBtn_Click"/>
                                    <Button Style="{StaticResource ChipButton}" Tag="1.1.1.1"       Content="1.1.1.1"       Click="TraceHizliBtn_Click"/>
                                    <Button Style="{StaticResource ChipButton}" Tag="192.168.1.1"   Content="192.168.1.1"   Click="TraceHizliBtn_Click"/>
                                    <Button Style="{StaticResource ChipButton}" Tag="google.com"    Content="google.com"    Click="TraceHizliBtn_Click"/>
                                </WrapPanel>
                            </StackPanel>
                            <Button x:Name="TraceBaslatBtn" Grid.Row="3" Style="{StaticResource PrimaryButton}"
                                    IsEnabled="False" Height="36" HorizontalContentAlignment="Center"
                                    Click="TraceBaslatBtn_Click" Margin="0,0,0,6">
                                <TextBlock Text="⇢  Rotayı Takip Et" FontFamily="Consolas" FontSize="12" FontWeight="Bold"/>
                            </Button>
                            <Border x:Name="TraceResultBorder" Grid.Row="4" Background="#0D1117"
                                    BorderBrush="#21262D" BorderThickness="1" CornerRadius="6"
                                    Margin="0,4,0,0" Visibility="Collapsed">
                                <ScrollViewer x:Name="TraceResultScroll" MaxHeight="200"
                                              VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                    <StackPanel x:Name="TraceResultPanel" Margin="10,8,10,8"/>
                                </ScrollViewer>
                            </Border>
                            <Button Grid.Row="5" Content="✕  Kapat" Style="{StaticResource ActionButton}"
                                    Click="TracePanelKapat_Click" Margin="0,10,0,0"
                                    HorizontalContentAlignment="Center" Foreground="#8B949E"/>
                        </Grid>
                    </Border>
                </Grid>

                <!-- DNS Lookup paneli -->
                <Grid Grid.Column="1" x:Name="DnsPanel" ClipToBounds="True" Visibility="Collapsed">
                    <Border Background="#0D1117" BorderBrush="#21262D" BorderThickness="1" CornerRadius="8" Margin="8,0,0,0">
                        <Grid Margin="14,14,14,14">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                                <TextBlock Text="⊕ DNS LOOKUP" Foreground="#58A6FF" FontFamily="Consolas" FontSize="12" FontWeight="Bold" Margin="0,0,0,6"/>
                                <TextBlock Text="Hostname↔IP çözümle:" Foreground="#8B949E" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"/>
                            </StackPanel>
                            <Grid Grid.Row="1" Margin="0,0,0,8">
                                <TextBox x:Name="DnsHedefBox" Style="{StaticResource PingInputBox}" Padding="10,8,10,8"
                                         TextChanged="DnsHedefBox_TextChanged" KeyDown="DnsHedefBox_KeyDown"/>
                                <TextBlock x:Name="DnsHedefPlaceholder" Text="IP veya hostname"
                                           FontFamily="Consolas" FontSize="12" Foreground="#484F58"
                                           VerticalAlignment="Center" Margin="12,0,0,0" IsHitTestVisible="False"/>
                            </Grid>
                            <Button x:Name="DnsBaslatBtn" Grid.Row="2" Style="{StaticResource PrimaryButton}"
                                    IsEnabled="False" Height="36" HorizontalContentAlignment="Center"
                                    Click="DnsBaslatBtn_Click" Margin="0,0,0,6">
                                <TextBlock Text="⊕  Çözümle" FontFamily="Consolas" FontSize="12" FontWeight="Bold"/>
                            </Button>
                            <Border x:Name="DnsResultBorder" Grid.Row="3" Background="#0D1117"
                                    BorderBrush="#21262D" BorderThickness="1" CornerRadius="6"
                                    Margin="0,4,0,0" Visibility="Collapsed">
                                <ScrollViewer x:Name="DnsResultScroll" MaxHeight="200"
                                              VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                    <StackPanel x:Name="DnsResultPanel" Margin="10,8,10,8"/>
                                </ScrollViewer>
                            </Border>
                            <Button Grid.Row="4" Content="✕  Kapat" Style="{StaticResource ActionButton}"
                                    Click="DnsPanelKapat_Click" Margin="0,10,0,0"
                                    HorizontalContentAlignment="Center" Foreground="#8B949E"/>
                        </Grid>
                    </Border>
                </Grid>

                <!-- SNMP Sorgusu paneli -->
                <Grid Grid.Column="1" x:Name="SnmpPanel" ClipToBounds="True" Visibility="Collapsed">
                    <Border Background="#0D1117" BorderBrush="#21262D" BorderThickness="1" CornerRadius="8" Margin="8,0,0,0">
                        <Grid Margin="14,14,14,14">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <!-- Başlık -->
                            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                                <TextBlock Text="⊙ SNMP SORGUSU" Foreground="#58A6FF" FontFamily="Consolas" FontSize="12" FontWeight="Bold" Margin="0,0,0,6"/>
                                <TextBlock Text="Cihaz sistem bilgilerini sorgula (v1/v2c):" Foreground="#8B949E" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"/>
                            </StackPanel>
                            <!-- IP giriş -->
                            <Grid Grid.Row="1" Margin="0,0,0,8">
                                <TextBox x:Name="SnmpIpBox" Style="{StaticResource PingInputBox}" Padding="10,8,34,8"
                                         TextChanged="SnmpIpBox_TextChanged" KeyDown="SnmpIpBox_KeyDown"/>
                                <TextBlock x:Name="SnmpIpPlaceholder" Text="Hedef IP"
                                           FontFamily="Consolas" FontSize="13" Foreground="#484F58"
                                           VerticalAlignment="Center" Margin="12,0,0,0" IsHitTestVisible="False"/>
                                <TextBlock x:Name="SnmpIpValidasyon" HorizontalAlignment="Right"
                                           VerticalAlignment="Center" Margin="0,0,10,0"
                                           FontFamily="Consolas" FontSize="15" FontWeight="Bold" IsHitTestVisible="False"/>
                            </Grid>
                            <!-- Community string -->
                            <Grid Grid.Row="2" Margin="0,0,0,8">
                                <TextBox x:Name="SnmpCommunityBox" Style="{StaticResource PingInputBox}" Padding="10,8,10,8"
                                         TextChanged="SnmpCommunityBox_TextChanged"/>
                                <TextBlock x:Name="SnmpCommunityPlaceholder" Text="Community: public"
                                           FontFamily="Consolas" FontSize="11" Foreground="#484F58"
                                           VerticalAlignment="Center" Margin="12,0,0,0" IsHitTestVisible="False"/>
                            </Grid>
                            <!-- Versiyon seçimi -->
                            <StackPanel Grid.Row="3" Margin="0,0,0,8">
                                <TextBlock Text="SNMP VERSİYONU" Foreground="#484F58" FontFamily="Consolas" FontSize="10" Margin="0,0,0,6"/>
                                <WrapPanel>
                                    <Button x:Name="SnmpV2cBtn" Style="{StaticResource SelectedChipButton}" Tag="v2c" Content="v2c" Click="SnmpVersiyonBtn_Click"/>
                                    <Button x:Name="SnmpV1Btn"  Style="{StaticResource ChipButton}"         Tag="v1"  Content="v1"  Click="SnmpVersiyonBtn_Click"/>
                                </WrapPanel>
                            </StackPanel>
                            <!-- Başlat -->
                            <Button x:Name="SnmpBaslatBtn" Grid.Row="4" Style="{StaticResource PrimaryButton}"
                                    IsEnabled="False" Height="36" HorizontalContentAlignment="Center"
                                    Click="SnmpBaslatBtn_Click" Margin="0,0,0,6">
                                <TextBlock Text="⊙  Sorgula" FontFamily="Consolas" FontSize="12" FontWeight="Bold"/>
                            </Button>
                            <!-- Sonuç -->
                            <Border x:Name="SnmpResultBorder" Grid.Row="5" Background="#0D1117"
                                    BorderBrush="#21262D" BorderThickness="1" CornerRadius="6"
                                    Margin="0,4,0,0" Visibility="Collapsed">
                                <ScrollViewer x:Name="SnmpResultScroll" MaxHeight="200"
                                              VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                    <StackPanel x:Name="SnmpResultPanel" Margin="10,8,10,8"/>
                                </ScrollViewer>
                            </Border>
                            <!-- Kapat -->
                            <Button Grid.Row="6" Content="✕  Kapat" Style="{StaticResource ActionButton}"
                                    Click="SnmpPanelKapat_Click" Margin="0,10,0,0"
                                    HorizontalContentAlignment="Center" Foreground="#8B949E"/>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Wake-on-LAN paneli -->
                <Grid Grid.Column="1" x:Name="WolPanel" ClipToBounds="True" Visibility="Collapsed">
                    <Border Background="#0D1117" BorderBrush="#21262D" BorderThickness="1" CornerRadius="8" Margin="8,0,0,0">
                        <Grid Margin="14,14,14,14">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                                <TextBlock Text="⏻ WAKE-ON-LAN" Foreground="#58A6FF" FontFamily="Consolas" FontSize="12" FontWeight="Bold" Margin="0,0,0,6"/>
                                <TextBlock Text="MAC adresine magic packet gönder:" Foreground="#8B949E" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"/>
                            </StackPanel>
                            <Grid Grid.Row="1" Margin="0,0,0,8">
                                <TextBox x:Name="WolMacBox" Style="{StaticResource PingInputBox}" Padding="10,8,34,8"
                                         TextChanged="WolMacBox_TextChanged" KeyDown="WolMacBox_KeyDown"/>
                                <TextBlock x:Name="WolMacPlaceholder" Text="MAC: AA:BB:CC:DD:EE:FF"
                                           FontFamily="Consolas" FontSize="11" Foreground="#484F58"
                                           VerticalAlignment="Center" Margin="12,0,0,0" IsHitTestVisible="False"/>
                                <TextBlock x:Name="WolMacValidasyon" HorizontalAlignment="Right"
                                           VerticalAlignment="Center" Margin="0,0,10,0"
                                           FontFamily="Consolas" FontSize="15" FontWeight="Bold" IsHitTestVisible="False"/>
                            </Grid>
                            <Button x:Name="WolGonderBtn" Grid.Row="2" Style="{StaticResource PrimaryButton}"
                                    IsEnabled="False" Height="36" HorizontalContentAlignment="Center"
                                    Click="WolGonderBtn_Click" Margin="0,0,0,6">
                                <TextBlock Text="⏻  Magic Packet Gönder" FontFamily="Consolas" FontSize="12" FontWeight="Bold"/>
                            </Button>
                            <Border x:Name="WolResultBorder" Grid.Row="3" Background="#0D1117"
                                    BorderBrush="#21262D" BorderThickness="1" CornerRadius="6"
                                    Margin="0,4,0,0" Visibility="Collapsed">
                                <ScrollViewer MaxHeight="120" VerticalScrollBarVisibility="Auto">
                                    <StackPanel x:Name="WolResultPanel" Margin="10,8,10,8"/>
                                </ScrollViewer>
                            </Border>
                            <Button Grid.Row="4" Content="✕  Kapat" Style="{StaticResource ActionButton}"
                                    Click="WolPanelKapat_Click" Margin="0,10,0,0"
                                    HorizontalContentAlignment="Center" Foreground="#8B949E"/>
                        </Grid>
                    </Border>
                </Grid>

            </Grid>
        </Grid>

        <!-- Dikey ayırıcı -->
        <Rectangle Grid.Column="0"
                   HorizontalAlignment="Right"
                   Width="1"
                   Fill="#21262D"
                   Margin="0,16,0,16"/>

        <!-- SAĞ: Kontrol paneli -->
        <Grid Grid.Column="1" Margin="8,16,16,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Panel başlığı -->
            <TextBlock Grid.Row="0"
                       Text="KONTROL"
                       Foreground="#8B949E"
                       FontSize="11"
                       FontWeight="Bold"
                       FontFamily="Consolas"
                       Margin="0,0,0,12"/>

            <!-- Butonlar -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>

                <!-- Taramayı Başlat -->
                <Button x:Name="BtnTaramaBaslat"
                        Style="{StaticResource PrimaryButton}"
                        Click="BtnTaramaBaslat_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="▶  " FontSize="13"/>
                        <TextBlock Text="Taramayı Başlat" FontSize="13"/>
                    </StackPanel>
                </Button>

                <!-- Taramayı Durdur -->
                <Button x:Name="BtnTaramaDurdur"
                        Style="{StaticResource DangerButton}"
                        IsEnabled="False"
                        Click="BtnTaramaDurdur_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="■  " FontSize="13"/>
                        <TextBlock Text="Taramayı Durdur" FontSize="13"/>
                    </StackPanel>
                </Button>

                <!-- Ayırıcı -->
                <Rectangle Height="1" Fill="#21262D" Margin="0,8,0,8"/>

                <!-- Ping Testi -->
                <Button x:Name="BtnPing"
                        Style="{StaticResource ActionButton}"
                        Click="BtnPing_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="◈  " FontSize="13"/>
                        <TextBlock Text="Ping Testi" FontSize="13"/>
                    </StackPanel>
                </Button>

                <!-- Port Tara -->
                <Button x:Name="BtnPortTara"
                        Style="{StaticResource ActionButton}"
                        Click="BtnPortTara_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⊞  " FontSize="13"/>
                        <TextBlock Text="Port Tara" FontSize="13"/>
                    </StackPanel>
                </Button>

                <!-- Traceroute -->
                <Button x:Name="BtnTrace"
                        Style="{StaticResource ActionButton}"
                        Click="BtnTrace_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⇢  " FontSize="13"/>
                        <TextBlock Text="Traceroute" FontSize="13"/>
                    </StackPanel>
                </Button>

                <!-- DNS Lookup -->
                <Button x:Name="BtnDns"
                        Style="{StaticResource ActionButton}"
                        Click="BtnDns_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⊕  " FontSize="13"/>
                        <TextBlock Text="DNS Lookup" FontSize="13"/>
                    </StackPanel>
                </Button>

                <!-- Ayırıcı -->
                <Rectangle Height="1" Fill="#21262D" Margin="0,8,0,8"/>

                <!-- Cihazları Listele -->
                <Button x:Name="BtnCihazlar"
                        Style="{StaticResource ActionButton}"
                        Click="BtnCihazlar_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="≡  " FontSize="13"/>
                        <TextBlock Text="Cihazları Listele" FontSize="13"/>
                    </StackPanel>
                </Button>

                <!-- ARP Tablosu -->
                <Button x:Name="BtnArp"
                        Style="{StaticResource ActionButton}"
                        Click="BtnArp_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="◫  " FontSize="13"/>
                        <TextBlock Text="ARP Tablosu" FontSize="13"/>
                    </StackPanel>
                </Button>

                <!-- Ağ Adaptörü -->
                <Button x:Name="BtnAgBilgi"
                        Style="{StaticResource ActionButton}"
                        Click="BtnAgBilgi_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⊛  " FontSize="13"/>
                        <TextBlock Text="Ağ Bilgisi" FontSize="13"/>
                    </StackPanel>
                </Button>

                <!-- Ayırıcı -->
                <Rectangle Height="1" Fill="#21262D" Margin="0,8,0,8"/>

                <!-- SADP -->
                <Button x:Name="BtnSadp"
                        Style="{StaticResource ActionButton}"
                        Click="BtnSadp_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⬡  " FontSize="13"/>
                        <TextBlock Text="SADP" FontSize="13"/>
                    </StackPanel>
                </Button>

                <!-- Wake-on-LAN -->
                <Button x:Name="BtnWol"
                        Style="{StaticResource ActionButton}"
                        Click="BtnWol_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⏻  " FontSize="13"/>
                        <TextBlock Text="Wake-on-LAN" FontSize="13"/>
                    </StackPanel>
                </Button>

                <!-- SNMP Sorgusu -->
                <Button x:Name="BtnSnmp"
                        Style="{StaticResource ActionButton}"
                        Click="BtnSnmp_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⊙  " FontSize="13"/>
                        <TextBlock Text="SNMP Sorgusu" FontSize="13"/>
                    </StackPanel>
                </Button>

                <!-- Ayırıcı -->
                <Rectangle Height="1" Fill="#21262D" Margin="0,8,0,8"/>

                <!-- Temizle -->
                <Button x:Name="BtnTemizle"
                        Style="{StaticResource ActionButton}"
                        Click="BtnTemizle_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⌫  " FontSize="13"/>
                        <TextBlock Text="Ekranı Temizle" FontSize="13"/>
                    </StackPanel>
                </Button>

            </StackPanel>
            </ScrollViewer>

            <!-- Alt: Versiyon bilgisi -->
            <TextBlock Grid.Row="2"
                       Text="v0.1.0 — Ağ Tarama"
                       Foreground="#484F58"
                       FontSize="10"
                       FontFamily="Consolas"
                       HorizontalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
